## របៀបប្រើប្រាស់
+ Start backend
- install from required ment python3 -m pip install -r requirements.txt (install all dependency)
- source .venv/bin/activate (active env mode)
- uvicorn app:app --reload (running application with uvicorn asynchronuse web server)
+ Front End (start with live server)
